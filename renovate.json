{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    ":dependencyDashboard"
  ],
  "kubernetes": {
    "enabled": true,
    "managerFilePatterns": [
      "/apps/*/deployment.yaml/",
      "/infrastructure/*/deployment.yaml/"
    ]
  },
  "helm-values": {
    "enabled": true,
    "managerFilePatterns": [
      "/apps/*/release.yaml/",
      "/infrastructure/*/release.yaml/"
    ]
  },
  "flux": {
    "enabled": true
  },
  "ignorePaths": [
    "clusters/homelab/flux-system/**"
  ],
  "customManagers": [
    {
      "customType": "regex",
      "description": "Track itzg/minecraft-server:java21 digest and VERSION env",
      "managerFilePatterns": ["apps/minecraft/deployment.yaml"],
      "matchStrings": ["image: itzg/minecraft-server:(?<currentValue>java21)", "VERSION: \"(?<currentValue>.*)\""],
      "datasourceTemplate": "docker",
      "depNameTemplate": "itzg/minecraft-server",
      "versioningTemplate": "docker"
    }
  ],
  "packageRules": [
    {
      "matchCategories": [
        "docker"
      ],
      "pinDigests": true
    },
    {
      "matchDatasources": ["docker"],
      "matchUpdateTypes": ["major"],
      "labels": ["docker-major"],
      "automerge": false
    }
  ]
}
