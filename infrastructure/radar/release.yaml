apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: radar
  namespace: radar
spec:
  interval: 1h
  chart:
    spec:
      chart: radar
      version: ">=1.0.0"
      sourceRef:
        kind: HelmRepository
        name: skyhook
        namespace: radar
  install:
    remediation:
      retries: 3
  values:
    ingress:
      enabled: true
      className: "traefik"
      hosts:
        - host: radar.zieqs.online
          paths:
            - path: /
              pathType: Prefix
